using UnityEngine;
using System.Collections;

public class simpleRotate : MonoBehaviour {

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
//		transform.Rotate (transform.TransformDirection(), 30f * Time.deltaTime);
	}
}
